import { AlumnoContactoDTO } from './alumno-contacto-dto';

describe('AlumnoContactoDTO', () => {
  it('should create an instance', () => {
    expect(new AlumnoContactoDTO()).toBeTruthy();
  });
});
